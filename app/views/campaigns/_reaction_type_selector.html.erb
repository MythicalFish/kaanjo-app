<% selected = @campaign.try(:reaction_type_ids) || [] %>
<%= f.collection_select( 
  :reaction_type_ids, 
  ReactionType.all, :id, :name, {   
    :selected => selected
  },{ :multiple => true } 
) %>