%img.kaanjo-status-icon{src:"http://#{site_url}#{reaction.scenario.image_url}"}
- number = product.reaction_total
- if reaction.scenario.message
  = reaction.scenario.message.sub('{number}',number.to_s)
